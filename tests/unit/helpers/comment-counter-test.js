
import { commentCounter } from 'blaahhg/helpers/comment-counter';
import { module, test } from 'qunit';

module('Unit | Helper | comment counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commentCounter([42]);
  assert.ok(result);
});

