<div class="layout-column layout-align-space-around-center">
  {{#each model as |post|}}
    {{post-tile post=post userid=session.currentUser.uid session=session saveComment='saveComment'}}
  {{/each}}
</div>
